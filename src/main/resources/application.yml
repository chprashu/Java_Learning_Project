server:
  port: 7500

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, matrics, beans, env, shutdown, loggers
  endpoint:
    health:
      show-details: always
  # server:
  #   port: 7503

 