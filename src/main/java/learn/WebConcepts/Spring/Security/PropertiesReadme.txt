Spring Properties can be confifured in .properties or .yml files

priority
    1. command line configuration
    2. .properties file configuration
    3. .yml file configuration

YAML (.yml files): ain't markup language

server:
    port: ->  assigning port the server where project is running
    servlet:
        context-path: -> starting path after port 